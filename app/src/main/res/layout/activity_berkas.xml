<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#F4F6FA"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- FILTER -->
    <HorizontalScrollView
        android:id="@+id/filterBox"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:scrollbars="none"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <!-- SEMUA -->
            <LinearLayout
                android:id="@+id/filterSemua"
                android:background="@drawable/bg_filter_active"
                android:padding="14dp"
                android:layout_marginEnd="10dp"
                android:layout_width="90dp"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:clickable="true"
                android:focusable="true">

                <TextView
                    android:id="@+id/tvJumlahSemua"
                    android:text="@string/jumlah_default"
                    android:textColor="#FFFFFF"
                    android:textStyle="bold"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>

                <TextView
                    android:text="@string/semua"
                    android:textColor="#FFFFFF"
                    android:textSize="12sp"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>
            </LinearLayout>

            <include layout="@layout/item_filter" android:id="@+id/filterSurat"/>
            <include layout="@layout/item_filter" android:id="@+id/filterLaporan"/>
            <include layout="@layout/item_filter" android:id="@+id/filterNotulen"/>

        </LinearLayout>
    </HorizontalScrollView>

    <!-- SEARCH -->
    <EditText
        android:id="@+id/etSearch"
        android:hint="@string/cari_berkas"
        android:background="@drawable/bg_search"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:padding="12dp"
        android:inputType="text"
        android:autofillHints=""
        app:layout_constraintTop_toBottomOf="@id/filterBox"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="12dp"/>

    <!-- LIST -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvBerkas"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingBottom="100dp"
        android:clickable="false"
        android:focusable="false"
        app:layout_constraintTop_toBottomOf="@id/etSearch"
        app:layout_constraintBottom_toTopOf="@id/bottomNav"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- NAVBAR -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="0dp"
        android:layout_height="70dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:menu="@menu/bottom_menu"/>

    <!-- BUTTON TAMBAH -->
    <ImageView
        android:id="@+id/btnTambah"
        android:src="@android:drawable/ic_input_add"
        android:background="@drawable/bg_btn_add"
        android:contentDescription="@string/tambah_berkas"
        android:padding="16dp"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:elevation="50dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintBottom_toTopOf="@id/bottomNav"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>